name: Release APK

on:
  push:
    tags: [ "v*" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Make gradlew executable
        run: chmod +x ./gradlew

      - name: Build debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Rename APK
        run: |
          mkdir -p out
          cp app/build/outputs/apk/debug/app-debug.apk out/fittrack-${{ github.ref_name }}-debug.apk

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: FitTrack ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          body: |
            FitTrack debug build — install on Android 8.0+ (enable "install unknown apps").

            This is a debug-signed APK for testing. 100% free & offline.
          files: out/fittrack-${{ github.ref_name }}-debug.apk
          fail_on_unmatched_files: true
